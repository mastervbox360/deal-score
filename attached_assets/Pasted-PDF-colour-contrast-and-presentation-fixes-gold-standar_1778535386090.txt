PDF colour, contrast and presentation fixes — gold standard audit
Review and fix the following in DealScorePDF.tsx. The goal is a gold standard investor pack where colour is used for structure and emphasis only — never where it makes text hard to read.
Fix 1 — BMV banner text contrast: The £amount and percentage in the BMV banner are rendering too light against the pale green background. Fix: £amount should always render in #1B2B4B (dark navy). The percentage should render in getReadableBrandColour(accentColour) to ensure it is dark enough to read. Never use raw accentColour directly on light backgrounds.
Fix 2 — Section headings consistency: All section headings across all pages (Executive Summary, Property Details, Financial Summary, Deal Inputs, Why This Strategy, Property Description, Comparable Properties, Property Listings, Property Photos, Legal & Disclosure, Vendor Situation, Sourcing Fee) must use getReadableBrandColour(brandColour) not raw brandColour. This ensures pale brand colours are darkened before use as text.
Fix 3 — Body text: Confirm all body text, table cell text, and label text is #333333. No body text should ever be in brand or accent colour.
Fix 4 — Comparable table header: Table header background should use getReadableBrandColour(brandColour). Header text must use getContrastText() against that background to always be readable.
Fix 5 — Listing link colour: "View on Rightmove >" link text should use getReadableBrandColour(accentColour) — not raw accentColour which may be too light.
Fix 6 — Deal Score badge: Increase the visual weight of the Deal Score badge on Page 3. It is the most important verdict in the document. Make the badge text bold and ensure the badge has sufficient padding and font size to command attention.
Fix 7 — Reinstate Property Description heading on Page 4: Reinstate "Property Description" as a bold section heading above the property description text — same visual style as "Why This Strategy?" heading. This is a PDF label, not content from the text field.
Fix 8 — Ghost comparable row: "65a Horwood Close" is consistently appearing as the third comparable row. Check whether any code in Home.tsx seeds the comparables array from the property address field. Comparables must initialise as three completely empty rows: [{address:'',bedsType:'',price:''},{address:'',bedsType:'',price:''},{address:'',bedsType:'',price:''}].
Fix 9 — Risk flags on Page 3: Risk flags are not rendering on Page 3. Find where risk flags are generated and ensure they appear on Page 3 below the Deal Score badge, only if active flags exist.
Do not touch cover styles, footer, photo grid, listing links UI, or AI summary. Run npx tsc --noEmit — zero errors required. Confirm exactly what you changed.