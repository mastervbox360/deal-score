Stage 6B — Investor Pack Content
We are building Stage 6B on the stage-6 branch. Do NOT touch main. Do NOT touch any code that is not directly related to the four features below. Confirm what you changed at the end.
File to modify: src/components/DealScorePDF.tsx and the relevant sections of Home.tsx (or whichever file holds the app state and UI inputs). Do not touch any other files unless strictly necessary.
Feature 1 — Unlimited Photo Upload (Smart PDF Grid)
In the app UI, add a photo upload section (accepts JPG/PNG, multiple files). Store photos as base64 in state (photoFiles: string[]). In DealScorePDF.tsx, render photos on a dedicated PDF page using this grid logic: 1 photo = full width, 2 photos = side by side (50% each), 3 photos = 3 across (33% each), 4+ photos = 3 per row wrapping. Each photo has a small caption slot (optional, blank by default). Photos section only appears in PDF if at least 1 photo is uploaded. Add a remove button per photo in the UI.
Feature 2 — Flexible Comparables (Add/Remove Rows)
Replace the existing static comparables text field with a dynamic table in the app UI. Each row has 3 columns: Address, Beds/Type, and Price. Default state: 3 empty rows. Add Row button adds a new empty row. Each row has a remove icon. In DealScorePDF.tsx, render the comparables as a clean 3-column table on PDF Page 4 (where Deal Notes already live). Table only appears in PDF if at least 1 row has content. The existing comparables textarea should be removed and replaced with this structured input.
Feature 3 — Executive Summary with AI Generation
Add an Executive Summary text area in the app UI (above the property description, on the Deal Notes section or branding panel — position TBD, use best judgment). Add a "Generate with AI" button next to it. On click, call the Anthropic API (claude-sonnet-4-20250514) with a prompt that includes: property address, strategy, purchase price, key metrics (yield, ROI, CF), deal score verdict, and why this strategy text. The API should return a 3–4 sentence executive summary. Display it in the text area — sourcer can edit freely. Free tier: limit to 3 total generations stored in localStorage (key: ds_ai_gen_count). When limit reached, show a message: "You've used your 3 free AI generations. Upgrade to Pro for unlimited." Pro and Pro Plus: unlimited. Executive Summary appears at the top of PDF Page 2 if populated. Do not hardcode any API key — use import.meta.env.VITE_ANTHROPIC_API_KEY and add the key to Replit Secrets as VITE_ANTHROPIC_API_KEY.
Feature 4 — Property Listing Links (Rightmove / Zoopla)
Add two URL input fields in the app UI (Deal Notes section or similar): Rightmove Link and Zoopla Link. Store as strings in state. In DealScorePDF.tsx, render these as clickable hyperlinks using React-PDF's Link component on PDF Page 4 (with comparables and deal notes). Label each link clearly. Only render a link in the PDF if the URL field is populated.
Do not change: cover styles, contrast system, footer layout, branding panel order, accent colour system, BRRR money-out logic, company name gating, PDF Preview modal, or any existing PDF pages beyond what is described above.
After building, run npx tsc --noEmit and confirm zero TypeScript errors. Then tell me exactly what files you changed and what you changed in each.