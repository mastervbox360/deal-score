Read src/components/Home.tsx in full before making any changes.

The accent colour rect elements in the three cover style thumbnail SVGs are positioned incorrectly — they do not match where the accent rule actually appears on each real cover page. Fix the position and width of the accent rect in each thumbnail to accurately reflect the real cover. Do not change brandColour usage, card structure, or any other element. Do not modify DealScorePDF.tsx or DealScorePDFProPlus.tsx.

────────────────────────────────────────
CLASSIC — portrait and landscape
────────────────────────────────────────

The real Classic cover: accent rule sits near the bottom of the full-bleed brand colour background, spanning most of the page width.

Portrait (viewBox 0 0 80 104):
  Replace existing accent rect with:
  <rect x="8" y="90" width="64" height="2" rx="1" fill={accentColour} opacity="0.9" />

Landscape (viewBox 0 0 120 88):
  Replace existing accent rect with:
  <rect x="8" y="74" width="104" height="2" rx="1" fill={accentColour} opacity="0.9" />

────────────────────────────────────────
CLEAN — portrait and landscape
────────────────────────────────────────

The real Clean cover: accent rule sits near the bottom of the white area, to the RIGHT of the left brand strip — it does not span the full width including the strip.

Portrait (viewBox 0 0 80 104):
  Replace existing accent rect with:
  <rect x="10" y="86" width="62" height="1.5" rx="0.75" fill={accentColour} opacity="0.9" />

Landscape (viewBox 0 0 120 88):
  Replace existing accent rect with:
  <rect x="11" y="72" width="101" height="1.5" rx="0.75" fill={accentColour} opacity="0.9" />

────────────────────────────────────────
BOLD — portrait and landscape
────────────────────────────────────────

The real Bold cover: accent rule sits on the LEFT dark brand panel near the bottom — not in the right white panel.

Portrait (viewBox 0 0 80 104):
  Remove any accent rect currently in the right panel area.
  Add accent rect in the left panel:
  <rect x="6" y="88" width="20" height="2" rx="1" fill={accentColour} opacity="0.9" />

Landscape (viewBox 0 0 120 88):
  Remove any accent rect currently in the right panel area.
  Add accent rect in the left panel:
  <rect x="6" y="74" width="32" height="2" rx="1" fill={accentColour} opacity="0.9" />

────────────────────────────────────────
VERIFY
────────────────────────────────────────

1. Run tsc --noEmit — fix all TypeScript errors before stopping.
2. Confirm DealScorePDF.tsx is untouched.
3. Confirm DealScorePDFProPlus.tsx is untouched.
4. git add -A && git commit -m "Cover thumbnails: accent colour positioned to match real cover pages" && git push origin stage-6