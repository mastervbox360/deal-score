Branch: production-candidate (confirm with git status first).
Behavior: after selecting an address via the (now-fixed) autocomplete, the "Property Intelligence" panel shows Flood risk data correctly, but never shows any EPC data — no rating, no band, nothing. Tested with two different postcodes (CF24 2LY, NG11 7BZ), same result both times.
Network tab observation: a request to epc-lookup?postcode=<POSTCODE> does fire each time (status 304, cache-revalidated, not an error), alongside land-registry?postcode=<POSTCODE> and a working floodAreas?lat=...&long=... call. I couldn't reliably capture the actual EPC response body through browser DevTools screenshots, so what that endpoint actually returns is still unconfirmed — please check server-side or test the route directly rather than guessing.
What to check: find the epc-lookup route/handler in the server code. Trace whether it calls an external EPC Register (gov.uk) API, whether that needs an API key/credential, and whether that credential is present/valid here. Test the route directly (curl, or a quick script) against a postcode you can independently verify has EPC data on record (https://find-energy-certificate.service.gov.uk/), to rule out the possibility that these two specific test postcodes simply have no EPC certificate on file (a real, non-bug scenario). If the external call is failing, fix it. If it's returning good data but the front end isn't rendering it, that's the same class of bug as the autocomplete issue — find where the Property Intelligence panel consumes the response and fix the rendering. Either way, if a postcode genuinely has no EPC record, the panel should say something like "No EPC data available for this property" instead of silently showing nothing — check whether that fallback exists and add it if not, matching the existing Flood risk empty-state style.
Report back: root cause, exact fix (or confirmation it's genuinely no-data, not a bug), whether no-data messaging was added, and any TypeScript errors. Do not push — report first.