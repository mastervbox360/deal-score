Stage 6B — Five refinements plus two outstanding bugs
Bug 1 — Blank photo pages: When many photos are uploaded, blank pages are generated between photo pages and before Legal & Disclosure. Fix the photo page chunking so pages continue seamlessly with no blank pages inserted anywhere.
Bug 2 — Risk flags not on Page 3: Risk flags are still not rendering on Page 3. Log the riskFlags value before the JSX return to confirm it is populated and fix whatever condition is preventing them from rendering.
Refinement 1 — Comparable Properties: add Date Sold column: Add a "Date Sold" text input field to each comparable row in Home.tsx (between Beds/Type and Price). Update the PDF table in DealScorePDF.tsx to show 4 columns: Address | Beds / Type | Date Sold | Price. Adjust column widths proportionally.
Refinement 2 — Photo lightbox in app UI: In Home.tsx, when a sourcer clicks a photo thumbnail in the Property Photos section, open a full-screen modal overlay showing the full image. The modal should have a close button (x) top right and a dark background overlay. This is an app UI feature only — no change to the PDF.
Refinement 3 — Singular/plural Property Listing heading: In DealScorePDF.tsx, count the number of populated listing link rows. If exactly 1 row has a URL, render the section heading as "Property Listing". If 2 or more rows have URLs, render as "Property Listings".
Refinement 4 — Deal Score badge redesign: Replace the large filled rectangle badge on Page 3 with a more refined treatment. Use a slim panel with a 4px left border in the verdict colour (green/amber/red), light tinted background, and the verdict text in bold navy. Verdict text should be 13pt bold. The panel should span full width but have modest padding — professional and understated, not dominant. Remove the large filled background entirely.
Refinement 5 — Deal Score label names: Change the three verdict labels everywhere they appear in the app (results panel, PDF badge, risk flags, tooltips) from "Strong Deal / Average Deal / Weak Deal" to "Recommended / Conditional / Not Recommended". The colour coding stays the same (green/amber/red). Update in both Home.tsx and DealScorePDF.tsx and anywhere else the labels are referenced.
Do not touch cover styles, footer, branding panel, AI summary, or any other feature. Run npx tsc --noEmit — zero errors required. Confirm exactly what you changed.