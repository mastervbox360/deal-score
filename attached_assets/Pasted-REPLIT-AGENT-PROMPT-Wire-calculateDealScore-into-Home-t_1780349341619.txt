REPLIT AGENT PROMPT — Wire calculateDealScore into Home.tsx
Read artifacts/dealscore/src/pages/Home.tsx in full before making any changes. Plan all edits explicitly before executing.
This prompt wires the new calculateDealScore function into Home.tsx so the numeric composite score is available everywhere the current currentScore (Strong/Average/Weak) is used. It does not change any existing calculation logic. It does not touch any PDF file or any other file.

TASK 1 — Import calculateDealScore
At the top of Home.tsx, find the existing import from '../lib/calculations'. Add calculateDealScore and DealScoreResult to that import.

TASK 2 — Compute the composite score
Find where currentScore is computed (it reads from the active strategy's .score property — Strong/Average/Weak/Incomplete). Immediately after that, add a new computed value compositeScore:
typescriptconst compositeScore: DealScoreResult | null = (() => {
  if (currentScore === 'Incomplete') return null;
  if (dealType === 'BTL') return calculateDealScore({
    strategy: 'BTL',
    cashOnCashROI: btlResults.cashOnCashROI,
    monthlyCashFlow: btlResults.monthlyCashFlow,
    grossYield: btlResults.grossYield,
  });
  if (dealType === 'HMO') return calculateDealScore({
    strategy: 'HMO',
    cashOnCashROI: hmoResults.cashOnCashROI,
    monthlyCashFlow: hmoResults.monthlyCashFlow,
    grossYield: hmoResults.grossYield,
  });
  if (dealType === 'FLIP') return calculateDealScore({
    strategy: 'FLIP',
    roi: flipResults.roi,
    netProfit: flipResults.netProfit,
    annualisedROI: flipResults.annualisedROI,
  });
  if (dealType === 'SA') return calculateDealScore({
    strategy: 'SA',
    cashOnCashROI: saResults.cashOnCashROI,
    monthlyCashFlow: saResults.monthlyCashFlow,
    netYield: saResults.netYield,
  });
  if (dealType === 'BRRR') return calculateDealScore({
    strategy: 'BRRR',
    cashLeftInDeal: brrrResults.cashLeftInDeal,
    moneyOut: brrrResults.moneyOut,
    monthlyCashFlow: brrrResults.monthlyCashFlow,
    cashOnCashROI: brrrResults.cashOnCashROI,
  });
  if (dealType === 'R2R') return calculateDealScore({
    strategy: 'R2R',
    monthlyProfit: r2rResults.monthlyProfit,
    roi: r2rResults.roi,
    spreadPerRoom: r2rResults.spreadPerRoom,
  });
  if (dealType === 'SOCIAL') return calculateDealScore({
    strategy: 'SOCIAL',
    cashOnCashROI: socialResults.cashOnCashROI,
    monthlyCashFlow: socialResults.monthlyCashFlow,
    grossYield: socialResults.grossYield,
  });
  return null;
})();

TASK 3 — Update the sticky Deal Score bar
Find where the Deal Score bar (the sticky strip below the header) renders the score label and colour. It currently uses getDealScoreSignal(currentScore) which returns colour and label from Strong/Average/Weak.
Update it so that when compositeScore is not null:

Show the numeric score: {compositeScore.score} in a large bold font
Show the verdict label: {compositeScore.verdict} — RECOMMENDED / REVIEW / AVOID
Keep the same colour logic (green for RECOMMENDED, amber for REVIEW, red for AVOID)

When compositeScore is null (incomplete): keep existing INCOMPLETE display unchanged.
The display should read: 74 — RECOMMENDED with the number prominent and the verdict label beside it.

TASK 4 — Update the score breakdown panel
Find the score breakdown section in the results panel (the collapsible or panel that shows the current Strong/Average/Weak breakdown with tick/cross rows).
Replace the tick/cross threshold display with a dimensions breakdown driven by compositeScore.dimensions:
tsx{compositeScore && compositeScore.dimensions.map((dim) => (
  <div key={dim.label} className="flex items-center gap-3 py-1.5 border-b border-border/30 last:border-0">
    <div className="flex-1 text-sm text-muted-foreground">{dim.label}</div>
    <div className="text-sm font-medium text-[#1B3A6B] w-16 text-right">
      {/* Format value appropriately — % for rates/yields, £ for cash values */}
      {dim.label.includes('Cash Flow') || dim.label.includes('Profit') || dim.label.includes('Cash Left') || dim.label.includes('Spread')
        ? `£${Math.round(dim.value).toLocaleString()}`
        : `${dim.value.toFixed(1)}%`}
    </div>
    <div className="text-xs text-muted-foreground w-20 text-right">{dim.strongThreshold}</div>
    <div className="w-24 bg-slate-100 rounded-full h-1.5 overflow-hidden">
      <div
        className="h-full rounded-full bg-[#1B3A6B]"
        style={{ width: `${Math.round((dim.points / dim.maxPoints) * 100)}%` }}
      />
    </div>
    <div className="text-xs font-medium text-[#1B3A6B] w-12 text-right">{dim.points}/{dim.maxPoints}</div>
  </div>
))}
{compositeScore && (
  <div className="flex items-center justify-between pt-2 mt-1 border-t border-border">
    <span className="text-sm font-semibold text-[#1B3A6B]">Total score</span>
    <span className="text-lg font-bold text-[#1B3A6B]">{compositeScore.score}/100</span>
  </div>
)}

TASK 5 — Update dealMetricsRef for Supabase
Find where dealMetricsRef.current is set (this stores the score for saving to Supabase). It currently stores dealScore: currentScore. Update it to also store the numeric score:
typescriptdealMetricsRef.current = {
  dealScore: compositeScore?.verdict ?? null,
  numericScore: compositeScore?.score ?? null,
  cashFlow: /* existing */,
  cocRoi: /* existing */,
  grossYield: /* existing */,
};
If numericScore is not in the existing type, add it as numericScore?: number | null.

AFTER ALL EDITS:
Run npx tsc --noEmit. Zero errors required.
Open a BTL deal on the Netlify preview with real inputs and confirm:

The sticky bar shows a number (e.g. "74 — RECOMMENDED") not just "RECOMMENDED"
The score breakdown panel shows the three dimensions with progress bars and pts/max
An incomplete deal still shows "INCOMPLETE" unchanged

Then run:
cd ~/workspace && git add -A && git commit -m "Wire calculateDealScore into Home.tsx — numeric score in sticky bar and breakdown panel" && git push origin stage-6
DO NOT TOUCH:

calculations.ts
Any PDF file
inputsSerializer.ts
dealService.ts
Any other file except Home.tsx

Report: TypeScript error count, screenshot of the sticky bar with a real deal loaded, screenshot of the score breakdown panel, push confirmation.