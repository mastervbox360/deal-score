Read Home.tsx in full before making any changes. Plan all 
edits explicitly, then execute in parallel. Confirm exactly 
what you changed at the end.

Do NOT touch DealScorePDF.tsx, DealScorePDFProPlus.tsx, 
or calculations.ts.

---

TASK 1 — FIX CASH FLOW TILE TO MATCH METRICBOX

Find the custom Cash Flow tile div for all 7 strategies 
(the div with Mo/Yr toggle buttons).

The MetricBox component renders with these exact styles 
(check the MetricBox component definition to confirm):
- Background: bg-slate-50 or similar grey
- Border: border border-border/60
- Padding: p-4
- Label: text-[10px] font-semibold uppercase tracking-wider 
  text-muted-foreground
- Value: text-2xl font-bold text-[#1B3A6B]

Update the Cash Flow tile outer div to exactly match 
MetricBox background and sizing:
- Add bg-slate-50 (or whatever background MetricBox uses — 
  read the MetricBox component first to confirm exact class)
- Ensure padding matches MetricBox exactly
- Ensure the value text size matches MetricBox value size
- Ensure min-height matches other tiles in the grid

The Mo/Yr toggle buttons stay inside the tile — just 
ensure the tile container background and sizing matches 
the other MetricBox tiles exactly.

Apply to all 7 strategies.

---

TASK 2 — FIX BTL FOURTH METRICBOX

Find the BTL MetricBox grid (grid-cols-2 gap-4).

Box 4 currently shows "Mortgage" (btlResults.mortgageAmount) 
— same as Box 2. Replace Box 4 with:

<MetricBox 
  label="Annual Flow" 
  value={hasMinimumData ? 
    formatCurrency(btlResults.annualCashFlow) : '—'} 
  highlight={hasMinimumData && 
    btlResults.annualCashFlow < 0} 
  tooltip={TT.annualFlow} 
/>

This gives the grid:
Box 1: Cash Invested
Box 2: Mortgage
Box 3: Cash Flow (toggle tile)
Box 4: Annual Flow

---

TASK 3 — TIGHTEN GAP BETWEEN METRICBOX GRID AND ROWS

Find the results panel for each strategy. Between the 
MetricBox grid (grid-cols-2 gap-4) and the rows section 
below it (div with space-y-3 containing Gross Yield, 
Net Yield, CoC ROI rows), there is currently a 
space-y-6 or similar large gap.

Reduce the outer space-y-6 wrapper to space-y-3 for 
the section containing the MetricBox grid and rows.

Also reduce the <div className="h-px bg-border" /> 
divider margin — change any mt-4 or my-4 on the 
divider to my-2.

Apply to all 7 strategies.

---

TASK 4 — SCORE BREAKDOWN CHEVRON: MORE PROMINENT

Find the Score Breakdown toggle button for all 7 
strategies.

Update the button styling:
- Change text colour from text-muted-foreground to 
  text-[#1B3A6B]/70
- Change text size from text-[9px] to text-[10px]
- Add font-medium
- ChevronDown icon: change w-3 h-3 to w-3.5 h-3.5
- Add a subtle hover background: 
  hover:bg-slate-50 rounded-lg transition-colors
- Keep the px-6 padding and full-width text-left

Apply to all 7 strategies.

---

TASK 5 — VERIFY

Run npx tsc --noEmit. Zero errors required.

Report:
1. Cash Flow tile background and sizing fixed — confirm 
   MetricBox background class used and applied to all 
   7 strategies (Task 1)
2. BTL Box 4 changed from Mortgage to Annual Flow (Task 2)
3. Gap reduced between MetricBox grid and rows — confirm 
   for all 7 strategies (Task 3)
4. Score Breakdown chevron updated — confirm for all 7 
   strategies (Task 4)
5. TypeScript error count (must be 0)