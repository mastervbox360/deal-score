I have everything I need. Here is the precise, copy-paste-ready Replit Agent prompt:

Two files to change: artifacts/dealscore/src/components/DealScorePDF.tsx and artifacts/dealscore/src/pages/Home.tsx (or wherever Home.tsx lives). Read both files in full before making any changes. Make exactly the changes below — nothing else.

CHANGE 1 — Home.tsx
1a — Add hero state directly after line const [photoFiles, setPhotoFiles] = useState<string[]>([]);:
typescriptconst [heroPhotoIndex, setHeroPhotoIndex] = useState<number>(0);
1b — Reset hero state in the reset handler, directly after setPhotoFiles([]);:
typescriptsetHeroPhotoIndex(0);
1c — Pass hero index into pdfProps in the useMemo, directly after photoFiles,:
typescriptheroPhotoIndex,
And add heroPhotoIndex to the useMemo dependency array, directly after photoFiles,.
1d — Replace the thumbnail grid — find this exact block:
tsx{photoFiles.map((src, i) => (
  <div key={i} className="relative group">
    <img src={src} alt={`Photo ${i + 1}`} className="w-full h-20 object-cover rounded-lg cursor-pointer" onClick={() => setLightboxPhoto(src)} />
    <button
      type="button"
      onClick={() => setPhotoFiles((prev) => prev.filter((_, j) => j !== i))}
      className="absolute top-1 right-1 bg-black/60 text-white rounded-full p-0.5 opacity-0 group-hover:opacity-100 transition-opacity"
      title="Remove photo"
    >
      <Trash2 className="w-3 h-3" />
    </button>
  </div>
))}
Replace with:
tsx{photoFiles.map((src, i) => (
  <div key={i} className="relative group">
    <img src={src} alt={`Photo ${i + 1}`} className="w-full h-20 object-cover rounded-lg cursor-pointer" onClick={() => setLightboxPhoto(src)} />
    {/* Hero badge */}
    {heroPhotoIndex === i ? (
      <span className="absolute top-1 left-1 bg-amber-400 text-white text-[9px] font-semibold px-1.5 py-0.5 rounded pointer-events-none">
        ★ Hero
      </span>
    ) : (
      <button
        type="button"
        onClick={() => setHeroPhotoIndex(i)}
        className="absolute top-1 left-1 bg-black/50 text-white text-[9px] px-1.5 py-0.5 rounded opacity-0 group-hover:opacity-100 transition-opacity"
        title="Set as hero photo"
      >
        ☆ Hero
      </button>
    )}
    <button
      type="button"
      onClick={() => {
        setPhotoFiles((prev) => {
          const next = prev.filter((_, j) => j !== i);
          // If removing the hero, reset to 0 (or clamp to last)
          if (heroPhotoIndex === i) setHeroPhotoIndex(0);
          else if (heroPhotoIndex > i) setHeroPhotoIndex((h) => h - 1);
          return next;
        });
      }}
      className="absolute top-1 right-1 bg-black/60 text-white rounded-full p-0.5 opacity-0 group-hover:opacity-100 transition-opacity"
      title="Remove photo"
    >
      <Trash2 className="w-3 h-3" />
    </button>
  </div>
))}

CHANGE 2 — DealScorePDF.tsx
2a — Add heroPhotoIndex to the props interface, directly after photoFiles: string[];:
typescriptheroPhotoIndex: number;
2b — Replace the photo splitting logic — find these exact lines:
typescriptconst validPhotos = props.photoFiles.filter(Boolean);
// Split into pages of 9 photos each
const photoPages: string[][] = [];
for (let i = 0; i < validPhotos.length; i += 9) {
  photoPages.push(validPhotos.slice(i, i + 9));
}
Replace with:
typescriptconst validPhotos = props.photoFiles.filter(Boolean);
const heroIdx = props.heroPhotoIndex ?? 0;
const heroPhoto = validPhotos[heroIdx] ?? validPhotos[0] ?? null;
const gridPhotos = validPhotos.filter((_, i) => i !== heroIdx);
// Split grid photos into pages of 9
const photoPages: string[][] = [];
for (let i = 0; i < gridPhotos.length; i += 9) {
  photoPages.push(gridPhotos.slice(i, i + 9));
}
2c — Replace the Page 2 Executive Summary block — find this exact block at the top of Page 2:
tsx{execSummaryText ? (
  <View style={base.notePanel}>
    <Text style={[base.notePanelLabel, { color: readableBrand }]}>Executive Summary</Text>
    <Text style={base.notePanelText}>{execSummaryText}</Text>
  </View>
) : null}

<SH title="Property Details" />
Replace with:
tsx{/* Two-column row: Executive Summary (left) + Hero photo (right) */}
<View style={{ flexDirection: 'row', gap: 12, marginBottom: 14 }}>
  {/* Left: Executive Summary (60%) */}
  <View style={{ flex: 0.6 }}>
    {execSummaryText ? (
      <View style={[base.notePanel, { marginBottom: 0 }]}>
        <Text style={[base.notePanelLabel, { color: readableBrand }]}>Executive Summary</Text>
        <Text style={base.notePanelText}>{execSummaryText}</Text>
      </View>
    ) : null}
  </View>
  {/* Right: Hero photo (38%) */}
  <View style={{ flex: 0.38 }}>
    {heroPhoto ? (
      <View>
        <Image
          src={heroPhoto}
          style={{ width: '100%', height: 180, objectFit: 'cover', borderRadius: 4 }}
        />
        <Text style={{ fontSize: 7.5, color: '#9ca3af', textAlign: 'center', marginTop: 4 }}>
          Property Preview
        </Text>
      </View>
    ) : null}
  </View>
</View>

<SH title="Property Details" />
2d — Replace validPhotos references in the photo pages map — the photo pages map already uses pagePhotos (derived from photoPages), and photoPages now uses gridPhotos, so no change needed there. Just verify the map still reads photoPages.map(...) — it should be unchanged.

DO NOT change:

Any other page layout (Page 1, 3, 4, 5, 7)
Any formula, scoring, or results logic
The notePanel style definition itself
The base.heroRow / base.heroCard metric hero component (that's the financial metrics hero, unrelated)
The lightbox portal in Home.tsx

Run npx tsc --noEmit after all changes. Zero errors required.
Confirm exactly what lines changed in each file.

After zero TS errors, push:
git add -A && git commit -m "Hero photo: Page 2 preview panel, grid uses remaining photos, hero badge in UI" && git push origin stage-6
Test checklist on stage-6--melodic-faun-882839.netlify.app:

0 photos → Page 2 right column empty, no errors
1 photo → Hero shows on Page 2, no grid pages at all
3 photos → Hero on Page 2, 2 photos in grid (1 page)
32 photos → Hero on Page 2, 31 in grid (4 pages, last page has 4 photos in 2 rows)
Click ☆ Hero on thumbnail 3 → badge moves, PDF hero updates
Remove the current hero photo → badge resets to index 0