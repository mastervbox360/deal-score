Stress Test repositioning

Read artifacts/dealscore/src/Home.tsx in full before making any changes. Plan all edits, then execute. Confirm what changed at the end.

WHAT TO CHANGE
The Stress Test is currently a standalone collapsible panel below the results Card. Move it inside the results Card, directly below the last metrics row (Gross Yield / Net Yield / Cash-on-Cash ROI / Equity on Day One), before the Card closes.
Remove the entire {stressSupported && ( <div className="mt-4">...</div> )} block that currently sits after </Card> at line ~2125. This is the standalone collapsible toggle and its contents — delete it entirely.
Add a stress test section inside the Card, after the last <Row> in each strategy tab but before the closing </div></Card> of the results panel. It should render when stressSupported is true.
New stress test UI inside the Card:

A subtle horizontal divider: <div className="border-t border-border mx-4 my-3" />
A small label row: SENSITIVITY ANALYSIS in text-[10px] font-semibold uppercase tracking-widest text-muted-foreground px-4 mb-2
The same 3-column table as before (Base Case / Rent −10% / Rate +1.5%) with the same two rows (Monthly Cash Flow / Cash-on-Cash ROI)
No toggle, no chevron, no collapsible — always visible when stressSupported is true
Same green/red colour logic on values
Compact row padding: py-2 px-4 to keep it tight within the card
Column headers: small muted text, right-aligned for the three value columns
Remove the stressTestOpen state usage entirely from this block — it is always shown

Also:

Remove setStressTestOpen(false) from handleReset since the state is no longer needed
Remove the stressTestOpen useState declaration at line ~150
Remove the setStressTestOpen toggle button entirely

Do NOT touch:

The Show Workings panel or its state
The includeWorkingsInPDF checkbox
The stressSupported, stressRentDown, stressRateUp computed values — keep all of these
The stressTest prop passed to pdfProps — keep as-is
DealScorePDF.tsx — no changes needed
generate-summary.cjs or any Netlify functions
Main branch — stage-6 only


AFTER BUILDING

Run npx tsc --noEmit — zero TypeScript errors required
Confirm exactly what was changed
Do NOT push to main. Do NOT merge branches.